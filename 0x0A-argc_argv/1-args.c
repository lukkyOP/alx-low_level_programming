#include <stdio.h>
#include "main.h"

/**
 * main - prints the number of arguments passed into it
 * @argc: argument counts
 * @argv arguments
 *
 * Return: 0 
 */

int main(int argc, *char argv[])
{
	(void) argv[]
	printf("%d\n", argc);
	return (0);
}
