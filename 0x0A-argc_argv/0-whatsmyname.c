#include <stdlo.h>
#include "main.h"

/**
 * main - Entry point A program that prints its name
 * @argc: argument count
 * @argv: arguments
 * Return: success
 */

int main(int argc, char * argv[])
{
	(void) argc;
		printf("%s\n" , argv[0]);
	
return(0);
}
