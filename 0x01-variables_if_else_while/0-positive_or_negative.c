#include <stdlib.h>
#include <time.h>
#include <studio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
        int n;


[A[A[A[B[A[B[B        strand(time(0));
        n = rand() - RAND_MAX / 2;

        if (n == 0)
                printf( "%d is zero\n", n);
        else if (n < 0)
                printf("%d[D[D[D [C"%d is negative\n", n);
        else
                print( "%d is positive\n", n);

        return (0);[A[D[D[D[D[D[D[D[D[D[D[A[A[C[C[C[Cif (n > 0)[B[B[B[B
